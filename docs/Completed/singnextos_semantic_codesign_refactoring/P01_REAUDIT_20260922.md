# P01 re-audit — owner and machine reconstruction

Date: 2026-09-22 (Europe/Moscow)

## 1. Disposition, claim, and gates

- Phase: P01.
- Disposition: `CLOSED_WITH_CORRECTIONS` for the locally executable SingNextOS owner/lifecycle contour.
- Claims: `RuntimeEnforced` only for the existing SingNext owner transitions exercised by local tests; `StaticAdmission` for architecture/ABI exclusions.
- All semantic co-design feature gates remain OFF. Ordinary behavior is unchanged.

## 2. Baseline and preserved worktree

- HEAD/tree before P01: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`.
- Pre-existing user-owned changes at the original P00 start: none.
- Dirty state entering P01 consisted only of this workstream's six P00 evidence/manifest files. They were preserved.
- Local packages and lock identities remain the exact P00 tuple. No remote source or network operation was used.

## 3. Requirement → owner → live behavior → test → evidence → claim

| Requirement | Authoritative owner | Live source/runtime behavior | Executed evidence | Claim/exclusion |
|---|---|---|---|---|
| CD-P01-01 owner/fact map | `CapabilityAuthority`, `ResourceBudgetAuthority`, `RegionAuthority`, session/invocation registries, `ExternalOperationAuthority`, response/publication owner; provider and HybridCPU runtime remain independent | Exact owner stores and mutation APIs remain in their existing classes; snapshots/descriptors declare no authority | canonical-store architecture regression plus capability/budget/Region/lifecycle tests | existing owner contour `RuntimeEnforced`; no global owner claim |
| CD-P01-02 transition trace | same per-fact owners | External operation: Prepared→Admitted→Submitted→DeviceComplete→Visible→Published→Released; budget: Reserved→Bound→Consuming→Settling→Released, with pre-submit cancellation and quarantine/reconcile; submit uses one `Interlocked.CompareExchange` winner and calls provider outside authority locks | 42 focused tests, including lifecycle/budget/Region/capability and new guards | exact local SingNext contour only |
| CD-P01-03 duplicate-owner/ABI guard | architecture/CI qualification owner | rejects named universal/duplicate semantic authorities, relocations of four canonical stores, authority mutation verbs on future semantic descriptor names, SingNext assembly identities and private topology in HybridCPU contracts ABI | 4 new tests passed; full non-GUI suite passed | `StaticAdmission`; ABI scan is not provider execution proof |

## 4. C1–C12 review

- C1/C4: capability/effect authority and resource budget are distinct owners; provider/package facts cannot authorize work.
- C3/C11: only `ResourceBudgetAuthority` mutates quantitative accounting; evidence and retirement do not own the ledger.
- C5/C6: publication remains with response/publication owners plus `ExternalOperationAuthority`; complete, visible, published, settlement, and release remain distinct.
- C7: operation, Region, budget, process/session and provider identities are generation-bound in existing live paths.
- C8/C9: receipts, evidence, replay and manifests expose no owner mutation path.
- C10: no global EffectEpoch or generic containment claim was introduced.
- C2/C12 are preserved constraints for P02–P06; P01 does not yet claim semantic refinement or four-gate enforcement.

## 5. Confirmed defects and remediation

- Confirmed gap: the tree lacked a semantic co-design-specific architecture guard against universal/duplicate owner types and SingNext identity leakage into the HybridCPU public contracts ABI.
- Remediation: added `SemanticCodesignOwnerBoundaryTests` with four source/reflection guards.
- During test development the first run failed 2/4 because two guards were overly broad: enum values such as `Published` were mistaken for authority methods, and provider-local endpoint names were mistaken for SingNext types. The guard was corrected to inspect callable methods on the exact future descriptor family and actual assembly/type identity rather than ambiguous substrings. Re-run passed 4/4.
- No live owner/runtime implementation defect requiring a production-code change was confirmed in P01.

## 6. Changed files by responsibility

- `tests/SingPlus.Tests/Architecture/SemanticCodesignOwnerBoundaryTests.cs`: executable owner-store, universal-authority, descriptor-mutation and HybridCPU ABI exclusions.
- This re-audit, tuple, task status, traceability, manifest and hashes: additive qualification evidence only.

## 7. Commands and actual results

- `git rev-parse HEAD`, `git rev-parse HEAD^{tree}`, `git status --short`: exact tuple and P00-owned dirty set recorded.
- Live `rg`/full-source review: existing owner stores and cross-owner state transitions located in the files named in the tuple.
- First focused test run: exit 1, 2 passed/2 failed due to overbroad new assertions; no production failure. Assertions were narrowed as described above.
- Corrected focused new guard run: exit 0, 4 passed.
- Focused owner/lifecycle run: exit 0, 42 passed, 0 failed, 0 skipped.
- `dotnet build SingNextOS.slnx --no-restore --configuration Debug`: exit 0, 0 warnings, 0 errors.
- Full non-GUI solution test lane with `FullyQualifiedName!~.Gui.`: exit 0. `SingPlus.Tests` 1563 passed/2 skipped; platform HybridCPU tests 60 passed; executable-adapter tests 90 passed; neutral-runtime tests 58 passed. Aggregate: 1771 passed, 2 skipped, 0 failed. The two skips are explicit suspended-child qualification probes, not hidden failures.

## 8. Fallback, blockers, and HybridCPU boundary

Ordinary behavior remains the only enabled path. Descriptors, receipts, evidence, tests and manifests remain non-authoritative.

FutureGated: P02 typed refinement, P04 obligations, P05 guarantees/binding, P06 four-gate sentry, provider/ISE source qualification and all hardware claims. Their owners and missing proofs remain those identified by the roadmap; bypassing them would merge authority with claims/evidence.

HybridCPU was used only through the local contracts package and external-tool/adapter test contour. No HybridCPU core, ISA, instruction encoding, VLIW, pipeline, registers, pointer model, typed lanes, compiler lowering, scheduler legality, retire architecture, memory controller, cache/coherence architecture, or microarchitecture changed. ISA impact: `NONE`.
