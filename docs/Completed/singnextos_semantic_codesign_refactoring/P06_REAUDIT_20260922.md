# P06 re-audit — generation-exact four-gate admission sentry

Date: 2026-09-22 (Europe/Moscow)

## 1. Phase/disposition/claim/gates

- Disposition: `CLOSED_HOST_COORDINATION_RUNTIME_PROVIDER_IMPLEMENTATIONS_FUTUREGATED`.
- Maximum claim: `RuntimeEnforced` only for the local SingNext sentry coordination, exact final owner revalidation, compensation boundary and one-winner transition exercised with test services. Provider admission and runtime-legality implementations are test doubles, so no `ExecutableAdapter`, provider/ISE execution, reservation, upper-bound or production claim follows.
- `FG-VNX-SEMANTIC-SENTRY` exists and is unconditionally OFF; every other vNext gate remains OFF.

## 2. Exact baseline and reviewed scope

- HEAD/tree: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`.
- P00 original user-owned dirty set: empty; all prior additive P00-P05 work was preserved.
- Exact local HybridCPU contract remains 1.14.0, nupkg SHA-256 `b96e99bda066ee585b26a11cbfa7b68ce6bf44fc0006679483ccc1a4eeb678c2`. No HybridCPU source tree or independent executable runtime-legality implementation exists locally.
- Reviewed: P06 phase/TZ, C1-C12, task/traceability rows, `ResourceAdmissionProtocol`, capability/budget/Region/session/external-operation owners, P04/P05 obligations/guarantees/binding, feature-gate policy and existing race/fault tests.

## 3. Requirement chain and disposition

| Requirement | Owner/live behavior | Executed evidence | Disposition |
|---|---|---|---|
| CD-P06-01 four gates | existing SingNext owners; provider service; pure refinement; independent runtime-legality service | positive conjunction and individual provider/refinement/runtime denial; exact binding revalidation | local sentry RuntimeEnforced; real provider/runtime services FutureGated |
| CD-P06-02 exact generation | immutable binding includes operation, digests, lease/envelope, provider identity, correlation and opaque generation set; decisions bind its digest | provider generation drift, tampered refinement and final owner reread reject | closed for host coordination |
| CD-P06-03 one submit winner | existing CAS submit-start transition; final sentry runs before it; loser never compensates after another winner | sequential replay and barrier-controlled concurrent race: one callback, one winner, lease Consuming | closed locally |
| CD-P06-04 races/faults | final reread covers live authority/session/lease/operation/Region state; sentry exceptions fail closed | capability revoke and session close during runtime legality, provider drift, service exception; all no-submit/pre-submit compensation | closed for tested owner races |

## 4. Defects and remediation

- Defect: no mandatory semantic conjunction seam in the resource-submit path. Added provider admission and independent runtime-legality interfaces, pure exact refinement proof, and final sentry callback.
- Defect: a sequential/concurrent duplicate could reach a failing final callback and attempt compensation after another submit winner. Added precheck plus winner-aware failure handling: after any winner, losers return `InvalidTransition` without refund.
- Defect: sentry exceptions escaped instead of failing closed. Non-fatal exceptions now become pre-submit `PlatformFaulted` compensation.
- Defect: obligations could only revalidate in `Prepared`; exact admitted operation/Region-use state is now accepted, while `Submitted` remains stale.
- Remaining risk: staged-output mutation-epoch semantics are not qualified here; P06 evidence uses read-only Region use. P08 owns visibility/Region/write semantics.

## 5. C1-C12 and separation

C1/C4/C8: provider/runtime decisions and proof explicitly remain non-authoritative; capability/resource owners are reread. C2/C12: pure dimension evaluator is recomputed and cannot be overridden by planner policy. C3/C11: exact bound lease/envelope is checked; measurement is not an input and no resource guarantee is claimed. C5/C6: no publication/visibility lifecycle was collapsed or authorized. C7: process, capability/resource generations, operation, Region/session, lease, provider identity/generation/correlation and binding digests fail closed. C9: duplicate/replay requires fresh gates and loses the existing CAS. C10: no containment claim exists.

## 6. Files, commands and actual results

Implementation: `SemanticAdmissionSentry.cs`, `ResourceAdmissionProtocol.cs`, `RuntimeKernel.SemanticObligations.cs`, `RuntimeKernel.SemanticExecutionBinding.cs`, `VNextFeatureGates.cs`; focused tests in `SemanticAdmissionSentryTests.cs` and adjusted obligation/binding regressions. Historical tuples/manifests received factual SHA/size corrections only where these live files were already pinned.

- focused semantic/architecture lane: 43 passed, 0 failed;
- solution build `dotnet build SingNextOS.slnx --no-restore`: exit 0, 0 warnings, 0 errors;
- full non-GUI: SingPlus 1610 passed/2 skipped; platform 60; adapter 90; neutral runtime 58. Aggregate 1818 passed, 2 skipped, 0 failed;
- the skipped suspended-child tests are the existing opt-in Windows qualification cases;
- JSON/manifest/hash and `git diff --check` are validated during evidence synchronization.

## 7. Fallback/FutureGated/exclusions

Ordinary admission is unchanged and is the only active fallback because the new gate is OFF. Missing prerequisites: exact executable provider admission service, independent HybridCPU runtime-legality implementation, source/version pin for that runtime, and end-to-end adapter evidence. Unsafe bypass: treating test doubles, package presence, compiler metadata, receipts or the binding as authority. Maximum provider/ISE claim remains Unsupported/StaticAdmission according to P05 dimensions.

HybridCPU was used only as a local external contract/tool boundary. No HybridCPU core, ISA, encoding, VLIW, pipeline, register, pointer, typed-lane, lowering, scheduler, retire, memory/cache/coherence or microarchitecture change was made. ISA impact: `NONE`.
