# P04 re-audit — immutable non-authoritative operation obligations

Date: 2026-09-22 (Europe/Moscow)

## 1. Phase, disposition, claim, and gates

- Phase: P04.
- Disposition: `CLOSED_WITH_FUTUREGATED_FINAL_SENTRY_WIRING`.
- `OperationObligationsV1`, its canonical digest, live snapshot composer and exact revalidator are implemented.
- Maximum phase claim: `StaticAdmission`. The revalidator is executable and race-tested, but it is not yet mandatory at every irreversible submit path; P06 owns that integration.
- All semantic co-design gates remain OFF. Ordinary fallback is unchanged.

## 2. Baseline and preserved changes

- HEAD/tree: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`.
- Original user-owned dirty set at P00: empty. P00–P03 implementation/evidence changes were preserved.
- P04 changed an input pinned by the historical vNext qualification tuple. Only its exact byte count/SHA and the containing historical manifest entry were factually corrected; historical disposition and claims were not changed.
- P00 package/DLL identities remain unchanged. No package presence was treated as provider behavior.

## 3. Requirement → owner → source → behavior → tests → claim

| Requirement | Owner | Live contract/source and behavior | Executed evidence | Claim |
|---|---|---|---|---|
| CD-P04-01 typed vocabulary | requirements contract only | versioned obligations reuse `ResourceEnvelopeV1`, P02 semantic requirements and existing effect/visibility/publication/Region types; IFC absent | canonical/unknown/null/tamper/reflection tests | `StaticAdmission` |
| CD-P04-02 live composition | existing Process, ExternalOperation, Region and optional EndpointSession owners | composer resolves exact live owner snapshots, captures Region generation+mutation epoch, clones collections and computes deterministic SHA-256 digest | positive composition and no-owner-mutation test | executable snapshot, not authority |
| CD-P04-03 revalidation | same existing owners | digest, process, Prepared operation, session, ordered Region uses, mutation epoch and temporal interval are re-resolved | Region mutation, session close, operation-state drift and expiry negatives | executable revalidator; final submit integration `FutureGated` |
| CD-P04-04 possession never authorizes | CapabilityAuthority/ResourceBudgetAuthority remain owners | obligations expose four explicit false authority flags and no mutation verbs | valid obligations plus forged/missing capabilities return `CapabilityNotFound`; operation stays Prepared and no lease appears | `StaticAdmission` anti-laundering proof |

Provider generation and exact budget lease are intentionally absent: neither exists at obligations construction time. They belong to P05 `SemanticExecutionBinding` and P06 final sentry. Adding them in P04 would create claims rather than snapshots.

## 4. C1–C12 review

- C1/C4/C8: obligations cannot authorize execution/effect/publication or reserve resources; possession negative passed.
- C2/C12: semantic requirements are immutable inputs only. No provider guarantee, planner or scheduler can mutate or satisfy them.
- C3/C11: resource requirements reuse canonical `ResourceEnvelopeV1`; no lease, accounting, measurement or charge is created.
- C5/C6: existing effect, visibility and publication requirements are captured without changing lifecycle state.
- C7: exact process/operation/session/Region generations, mutation epochs, temporal bounds and digest fail closed. Provider/lease generations remain P05/P06.
- C9: replay is a requirement class and cannot submit work.
- C10: containment is a requirement only; no global closure claim was added.

## 5. Defects, remediation, and remaining risks

- Gap: no aggregate immutable obligations contract. Remediated with an additive versioned contract and deterministic canonical digest.
- Gap: separate owner facts could be copied without an exact live composer. Remediated with a composer reading owners directly; caller-supplied Region epochs are not accepted.
- Gap: possession could be mistaken for permission. Remediated with explicit false flags, no mutation methods and live negative authorization test.
- Gap: snapshot staleness lacked one revalidation operation. Remediated for process/operation/session/Region/time. Remaining blocker: it is not yet compulsory at submit.
- Initial full-suite run exposed a stale historical tuple for the intentionally modified authority source. Exact size/hash and its manifest entry were corrected, then affected tests and the full suite were rerun successfully.

## 6. Changed files by responsibility

- `contracts/SingPlus.Contracts/OperationObligations.cs`: immutable vocabulary, validation, collection freezing and digest.
- `src/Runtime/SingPlus.Runtime/VNext/RuntimeKernel.SemanticObligations.cs`: exact live composer/revalidator.
- `src/Runtime/SingPlus.Runtime/ExternalOperations/ExternalOperationAuthority.cs`: internal read-only preparation query; no new state/owner.
- P04 contract/runtime tests, additive evidence/tuple and synchronized traceability/manifests.
- Historical vNext tuple/manifest: factual hash correction only.

## 7. Commands and actual results

- Phase-entry `git rev-parse HEAD` and `git status --short` executed; HEAD above and original user-owned dirty set remains empty.
- Focused P04 tests: 9 passed, 0 failed, 0 skipped.
- Expanded semantic/authority lane: 19 passed, 0 failed, 0 skipped.
- `dotnet build SingNextOS.slnx --no-restore --configuration Debug`: exit 0, 0 warnings, 0 errors.
- First full run: two historical hash-policy failures after the source edit; exact failures recorded and corrected, not skipped.
- Corrected affected lane: 2 passed.
- Final full non-GUI run: `SingPlus.Tests` 1591 passed/2 skipped; platform HybridCPU 60; executable adapter 90; neutral runtime 58. Aggregate: 1799 passed, 2 skipped, 0 failed.
- JSON, manifest/SHA validation and `git diff --check` are recorded after synchronization.

## 8. Fallback, FutureGated blockers, and HybridCPU exclusions

All gates remain OFF and ordinary admission remains available. Callers cannot use obligations in place of existing capability, budget, Region, session, operation or publication checks.

FutureGated owners: P05 must add exact provider guarantees/binding; P06 must make obligations digest and all relevant generations mandatory at the final four-gate sentry. Missing proof: enforced no-bypass submit integration and provider/lease drift races. Safe fallback: current admission path with no semantic-co-design claim.

HybridCPU scope remained local external-tool/package/adapter only. No core, ISA, encoding, VLIW, pipeline, registers, pointer model, typed lanes, compiler lowering, scheduler legality, retire, memory controller, cache/coherence or microarchitecture changes. ISA impact: `NONE`.
