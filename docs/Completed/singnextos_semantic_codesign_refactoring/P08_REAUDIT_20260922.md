# P08 re-audit — staged visibility, Region epochs and semantic locality

Date: 2026-09-22 (Europe/Moscow)

## 1. Phase/disposition/claim/gates

- Disposition: `CLOSED_STAGED_SUBSET_DIRECT_AND_SHARED_MUTABLE_FUTUREGATED`.
- Visibility/locality descriptors and locality filtering: `StaticAdmission`; existing Region use/mutation-epoch and ExternalOperation visibility/publication checks: `RuntimeEnforced` for the host/JIT staged contour.
- Direct coherent and shared mutable Region modes remain explicitly `PlatformUnsupported`; no alias/coherence claim.
- All rollout gates remain OFF.

## 2. Baseline and reviewed scope

- HEAD/tree: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`; original P00 user dirty set empty.
- Reviewed P08/TZ, C1-C12, task/traceability, `RegionAuthority`, Region contracts, external lifecycle, compute contracts/planner, direct-gate and lifecycle tests.
- HybridCPU package remains exact local 1.14.0 (`b96e99bda066ee585b26a11cbfa7b68ce6bf44fc0006679483ccc1a4eeb678c2`); no provider/runtime source proof.

## 3. Requirement chain

| Requirement | Owner/live behavior | Evidence | Disposition |
|---|---|---|---|
| CD-P08-01 visibility mapping | immutable descriptor maps five semantic classes to existing Region modes, visibility requirement and publication policy; owners unchanged | canonical/unknown/tamper tests; Complete-not-Visible and mutation-drift publication negative | staged subset closed |
| CD-P08-02 direct coherent | RegionAuthority rejects `DirectCoherentWrite`; planner probes therefore cannot legalize it | direct mode `PlatformUnsupported`; P07 proves no staged-withholding callback | FutureGated pending executable alias exclusion/coherent release |
| CD-P08-03 shared mutable | RegionAuthority rejects `SharedReadMostly`; no fractional/atomic ledger added | explicit `PlatformUnsupported` regression | Deferred, permitted fallback is staged/exclusive existing modes |
| CD-P08-04 locality | optional semantic requirement/provider guarantee with exact provider generation; planner only filters and revalidates | mandatory exact-class selection, wrong/unsupported/stale-generation rejection, portable surface check | StaticAdmission policy only |

## 4. Defects/remediation and remaining risks

- Gap: no typed distinction among immediate/acquire/staged/provider-private/direct visibility meanings. Added `ExternalVisibilitySemanticsV1` as a strict non-authoritative mapping.
- Gap: locality was latency/bandwidth ranking only. Added dimension-specific semantic requirement/guarantee; mandatory mismatch cannot be selected and exact provider generation is revalidated.
- No shared-mutable authority was created because no accepted critical-path contour requires it.
- Remaining: no CPU alias-exclusion mechanism, symmetric coherent release, real provider locality enforcement, or direct/coherent end-to-end evidence. Hardware topology stays provider-private.

## 5. C1-C12

C1/C8: descriptors and guarantees authorize nothing. C2/C12: locality filters candidates only and cannot override sentry refinement. C3/C4/C11: no accounting/effect authority changes. C5: staged maps to exact publication decision; direct maps without withheld gate but remains unqualified. C6: Complete and Visible remain separate. C7: Region handle/generation/use/mutation epoch and provider locality generation are exact. C9: evidence cannot resubmit. C10: no containment/global coherence claim.

## 6. Files and actual qualification

- `ExternalOperations.cs`: visibility vocabulary/mapping.
- `ComputePlanning.cs` and `ComputePlanner.cs`: optional portable locality policy/claim and mandatory filtering/revalidation.
- `VisibilityRegionLocalityV1Tests.cs`: malformed mapping, lifecycle/epoch, direct/shared gates, locality and portable API regressions.

Results:

- focused P08/Region/planner/lifecycle lane: 36 passed;
- solution build: exit 0, 0 warnings, 0 errors;
- full non-GUI: SingPlus 1620 passed/2 skipped; platform 60; adapter 90; neutral runtime 58; aggregate 1828 passed, 2 skipped, 0 failed;
- historical pinned hashes corrected factually; JSON/manifests and `git diff --check` validated during synchronization.

## 7. Fallback/FutureGated/HybridCPU exclusions

Ordinary staged Region output remains the safe fallback. Direct coherent, shared mutable/atomic, hardware-specific locality and actual provider enforcement remain FutureGated/Unsupported with RegionAuthority/provider owners and missing executable proof explicitly identified.

HybridCPU core, ISA, encoding, VLIW, pipeline, registers, pointers, typed lanes, compiler lowering, scheduler/retire architecture, memory/cache/coherence and microarchitecture were not changed. ISA impact: `NONE`.
