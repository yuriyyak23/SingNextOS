# P03 re-audit — global operational semantics without a new owner

Date: 2026-09-22 (Europe/Moscow)

## 1. Phase, disposition, claim, and gates

- Phase: P03.
- Disposition: `CLOSED_WITH_LIMITED_FORMAL_QUALIFICATION`.
- `CD-P03-01`: closed as a specification-only product view; no runtime `GlobalStateManager` exists.
- `CD-P03-02`: closed for the exact existing `ResourceAdmissionProtocol` contour, whose reversible and irreversible boundaries were re-executed.
- `CD-P03-03`: model authored and statically bound to concrete names, but TLC execution is `FutureGated` because no local `tla2tools` or Apalache executable/JAR exists.
- Maximum claims: `ModelOnly` for the TLA+ state space; `RuntimeEnforced` only for the existing resource-admission commit/compensation contour. All semantic co-design gates remain OFF.

## 2. Baseline and preserved dirty tree

- Current HEAD/tree: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`.
- Original user-owned dirty set at P00: empty.
- Entering P03, the dirty set consisted only of additive P00–P02 evidence, P01/P02 tests and the P02 contract implementation. It was preserved.
- Exact local package/lock tuple from P00 is unchanged. Package presence is not provider, runtime-legality, ISE or hardware proof.

## 3. Requirement → owner → source → behavior → tests → evidence → claim

| Requirement | Authoritative owner | Live source/runtime behavior | Executed evidence | Claim/exclusions |
|---|---|---|---|---|
| CD-P03-01 GlobalState product | each existing owner remains authoritative; model owns no facts | `OwnerStateProduct` projects capability, session, Region, budget, provider and operation state; claims remain non-authoritative | architecture test rejects a runtime manager and requires the product fields | `ModelOnly`; no lock, store or mutation API added |
| CD-P03-02 commit/compensation | existing Capability/ResourceBudget/Region/ExternalOperation owners | `TryStartSubmit` has one winner; durable `PossibleSubmit` precedes provider callback; failures before possible submit compensate, ambiguity afterward quarantines without refund | callback-under-owner-access, duplicate-submit, pre-boundary fault, provider-failure and post-boundary fault tests passed | `RuntimeEnforced` only for this existing host contour; not the future four-gate sentry |
| CD-P03-03 race model | specification only; live owners decide runtime state | TLA+ actions cover capability revoke, session/Region/lease/provider generation drift, provider admission, refinement, independent runtime legality, final revalidation, one winner, possible submit and quarantine | model/source consistency tests passed; existing revoke/Region/concurrent/provider-loss regressions passed | model not TLC-checked; no liveness claim and no generated counterexample claim |

## 4. C1–C12 review

- C1/C2/C12: the model requires separate SingNext authorization, provider admission, refinement and runtime legality before its submit winner; claims/planner do not authorize. P06 runtime wiring remains gated.
- C3/C4/C11: budget generation is one component of the owner product, not effect permission; no measurement or charging rule was added.
- C5/C6: `SubmitStarted`, `PossibleSubmit`, `Submitted`, `Quarantined` and `CancelledPreSubmit` remain distinct. Publication is intentionally outside this P03 model subset and receives no synthetic gate.
- C7: five relevant generation drifts are explicit and exact revalidation fails closed in the model; broader binding enforcement remains P04–P06.
- C8/C9: claims and replay evidence are not transitions and cannot start submission.
- C10: ambiguity is pinned only for the exact modeled operation/provider contour; no global containment or EffectEpoch is claimed.

## 5. Defects/remediation and remaining risks

- Confirmed gap: there was no common formal product-state vocabulary. Remediation: added the safety-only TLA+ model and finite configuration.
- Confirmed gap: roadmap wording could conflate the local CAS winner with external-effect possibility. Remediation: modeled `StartSubmitWinner` separately from durable `RecordPossibleSubmit`; compensation is allowed only while `possibleSubmit=FALSE`.
- Confirmed gap: model drift from concrete code was unguarded. Remediation: architecture tests bind formal actions/invariants to concrete qualification points and source ordering.
- Remaining risk: the local environment has Java but no TLC/Apalache artifact or executable. Unsafe bypass: treating text presence/static tests as exhaustive state exploration. Safe fallback: model remains `ModelOnly`; ordinary runtime path and gates OFF.
- Remaining risk: the complete four-gate final sentry and exact immutable binding do not yet exist. Owners/phases: P04–P06. No P03 document or model upgrades this claim.

## 6. Changed files by responsibility

- `formal/SemanticAdmissionSentry.tla` and `.cfg`: spec-only owner-product composition, race actions, safety invariants and explicit fault assumptions.
- `tests/SingPlus.Tests/Architecture/SemanticAdmissionFormalModelTests.cs`: model/source/name/order consistency regressions.
- Additive P03 evidence/tuple and synchronized task, traceability, package manifest and SHA256 index.

## 7. Commands and actual results

- `git rev-parse HEAD`; `git status --short`: HEAD above; only this roadmap implementation's additive/modified files, original user-owned set empty.
- Exact phase/TZ/live-owner source and existing cross-owner race tests were reviewed before editing.
- Tool discovery: Java present; no `tla2tools`, TLC JAR, `apalache-mc` or Apalache JAR found locally. No network lookup/download was performed.
- Focused P03 lane: 19 passed, 0 failed, 0 skipped.
- `dotnet build SingNextOS.slnx --no-restore --configuration Debug`: exit 0, 0 warnings, 0 errors.
- Full non-GUI solution lane: `SingPlus.Tests` 1582 passed/2 skipped; platform HybridCPU 60 passed; executable adapter 90 passed; neutral runtime 58 passed. Aggregate: 1790 passed, 2 skipped, 0 failed.
- JSON parsing, manifest/hash validation and `git diff --check` are recorded after synchronization.

## 8. Fallback, FutureGated blockers, and HybridCPU exclusions

Ordinary resource admission remains unchanged and all semantic co-design gates remain OFF. The formal files are non-executable authority and cannot authorize, submit, publish, refund or reconcile anything.

FutureGated: exhaustive TLC/Apalache model checking (missing local executable artifact); complete session/provider/binding races at the P06 sentry; staged publication composition; any liveness result. Timeout is never proof of no external effect.

HybridCPU scope remained local external-tool/package/adapter inspection only. No HybridCPU core, ISA, instruction encoding, VLIW, pipeline, registers, pointer model, typed lanes, compiler lowering, scheduler legality, retire architecture, memory controller, cache/coherence or microarchitecture changed. ISA impact: `NONE`.
