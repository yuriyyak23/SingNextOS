# P02 re-audit — typed semantic vocabulary and refinement algebra

Date: 2026-09-22 (Europe/Moscow)

## 1. Disposition, claim, and gates

- Phase: P02.
- Disposition: `CLOSED_WITH_CORRECTIONS` for the provider-neutral SingNext contract/evaluator contour.
- Maximum claim: `StaticAdmission`. The evaluator is executable and tested but is not yet wired to the irreversible submit boundary; that is P05/P06 work.
- All semantic co-design gates remain OFF; ordinary fallback is unchanged.

## 2. Baseline and preserved changes

- HEAD/tree: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`.
- Original user-owned dirty set: empty. Entering P02, dirty files were only additive P00/P01 evidence and the P01 architecture test; all were preserved.
- Exact P00 package/lock tuple remains unchanged. HybridCPU source was not available locally, so no source-level HybridCPU contract/runtime claim was made.

## 3. Requirement → owner → source → runtime behavior → test → claim

| Requirement | Owner | Live implementation | Executed tests | Claim |
|---|---|---|---|---|
| CD-P02-01 dimension-specific partial orders | pure provider-neutral contract/evaluator, never an authority | `SemanticPartialOrdersV1` defines explicit relations for isolation, publication enforcement, replay, determinism, cancellation, containment, locality and existing `ResourceAssuranceV1`; no enum ordinal comparison | exhaustive reflexivity/transitivity and incomparable-branch tests | `StaticAdmission` |
| CD-P02-02 Mandatory/Advisory/Unsupported | pure semantic contract | versioned generic requirement/guarantee records; Mandatory+Unsupported rejects, Advisory mismatch is explicitly unmet, and neither authorizes execution/effect | positive, mismatch, unsupported and unknown-value negatives | `StaticAdmission` |
| CD-P02-03 monotonicity/version failure | pure evaluator | `SemanticRefinementEvaluatorV1` validates versions, strengths, support and enum classes before comparison; unknown values return `InvalidContract` | exhaustive finite state exploration over every defined class | `StaticAdmission`; final sentry remains FutureGated |

## 4. C1–C12 review

- C1/C4/C8: requirements, guarantees and decisions expose `AuthorizesExecution=false` and `AuthorizesEffect=false`; they mutate no owner.
- C2: pure typed refinement now exists, but irreversible-execution enforcement is not claimed until P06 wires it.
- C3/C11: resource quantities reuse `ResourceEnvelopeV1`; assurance comparison reuses `ResourceAssuranceV1`; no second ledger or retired-work accounting was introduced.
- C5/C6: publication relation describes enforcement strength only; it does not advance lifecycle or make a publication decision.
- C7: versions fail closed; generation exactness remains P04–P06.
- C9: replay branches are compatibility claims only and cannot submit work.
- C10: containment has only `None` and exact operation/provider-generation closure; no global claim or EffectEpoch exists.
- C12: evaluator has no planner/scheduler override input.

## 5. Defects, remediation, and remaining risks

- Confirmed gap: no end-to-end typed refinement evaluator existed.
- Confirmed gap: absence/unsupported semantics and requirement strength were not uniform.
- Confirmed gap: replay and locality include incomparable branches that enum ordinal comparison would incorrectly launder.
- Remediation: added immutable versioned vocabulary, explicit dimension relations and a pure evaluator; reused existing resource types.
- Remaining risk: class mappings from HybridCPU Contracts 1.14.0 and runtime/provider evidence do not yet exist. Owner: P05 compatibility adapter/provider guarantee contour. Missing proof: local HybridCPU source plus executable mapping evidence. Safe fallback: `Unsupported`; gates OFF.

## 6. Changed files

- `contracts/SingPlus.Contracts/SemanticRefinementContracts.cs`: immutable non-authoritative vocabulary, explicit relations and evaluator.
- `tests/SingPlus.Tests/Contracts/SemanticRefinementV1Tests.cs`: finite-state/property, unknown-value and anti-laundering regressions.
- Additive P02 evidence/tuple plus synchronized task, traceability, manifest and hash files.

## 7. Commands and actual results

- Baseline/status and exact phase/TZ/live source review completed before edits.
- Focused semantic/resource/owner guard lane: 21 passed, 0 failed, 0 skipped.
- `dotnet build SingNextOS.slnx --no-restore --configuration Debug`: exit 0, 0 warnings, 0 errors.
- Full non-GUI solution lane: `SingPlus.Tests` 1569 passed/2 skipped; platform HybridCPU 60 passed; executable adapter 90 passed; neutral runtime 58 passed. Aggregate: 1777 passed, 2 skipped, 0 failed.
- JSON, manifest/hash and `git diff --check` validation are recorded after evidence synchronization.

## 8. Fallback, FutureGated blockers, and HybridCPU exclusions

The new evaluator is not a gate and not authority. Until P04–P06, ordinary admission remains the sole enabled path. Unknown/unsupported provider dimensions safely reject Mandatory requirements or remain explicit Advisory unmet results.

FutureGated: HybridCPU 1.14.0 semantic mapping, `ExecutionGuaranteesV1`, exact binding, final sentry and runtime-legality composition. Package presence and adapter tests are not provider/ISE behavior proof.

HybridCPU scope remained local external-tool/package/adapter only. No core, ISA, instruction encoding, VLIW, pipeline, registers, pointer model, typed lanes, compiler lowering, scheduler legality, retire architecture, memory controller, cache/coherence architecture or microarchitecture changed. ISA impact: `NONE`.
