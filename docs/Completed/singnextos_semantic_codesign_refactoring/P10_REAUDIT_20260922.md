# P10 re-audit — atomic reservation and non-authoritative scheduling

Date: 2026-09-22 (Europe/Moscow)

## 1. Phase/disposition/claim/gates

- Disposition: `CLOSED_EXISTING_ATOMIC_OWNER_EXACT_COMPUTETIME_MAPPING_PROVIDER_LOCAL_FUTUREGATED`.
- Existing `ResourceBudgetAuthority` vector reservation: `RuntimeEnforced`; semantic envelope mapping: `StaticAdmission`; scheduler hint generation/live-generation revalidation: `RuntimeEnforced` policy mechanics but never authority.
- No banker, escrow, second capacity ledger or scheduler admission authority was added. All rollout gates remain OFF.
- No `GuaranteedReservation`, provider-resource guarantee, performance or production claim is made.

## 2. Baseline and reviewed scope

- HEAD/tree: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`; original P00 user-owned dirty set empty.
- Reviewed P10/TZ, C1-C12, ResourceBudgetAuthority/contract, donation protocol, scheduler, exact semantic binding, recovery and focused owner tests.
- HybridCPU local package remains 1.14.0 with SHA-256 `b96e99bda066ee585b26a11cbfa7b68ce6bf44fc0006679483ccc1a4eeb678c2`; it does not prove provider-local atomic acquisition.

## 3. Requirement chain

| Requirement | Owner/live behavior | Evidence | Disposition |
|---|---|---|---|
| CD-P10-01 reuse atomic vector reservation | ResourceBudgetAuthority validates the full vector under its existing gate, checks every ancestor/dimension, then mutates; pure mapper feeds one existing Reserve/Split call | multi-dimension failure leaves all Used zero; canonical success; mapper malformed/duplicate tests | RuntimeEnforced owner; exact ComputeTime mapping closed |
| CD-P10-02 no banker/escrow | no new state machine/ledger; unsupported provider-local envelope in a set rejects the whole mapping before Reserve | six provider-local class negatives return PlatformUnsupported with no result | closed correction; provider all-or-nothing acquisition FutureGated |
| CD-P10-03 donation ceilings | existing capability delegation, SplitLease conservation, assurance/priority narrowing retained; common mapper replaces literals | nested widening/assurance/priority and parallel-split anti-amplification tests | RuntimeEnforced existing host contour |
| CD-P10-04 scheduler cannot legalize mismatch | scheduler observes/ranks and revalidates exact generations only; pure refinement/final sentry remains independent | stale/poisoned/restart evidence and favorable-score mandatory isolation mismatch | RuntimeEnforced separation for local policy path |

## 4. Defects/remediation and remaining risks

- Gap: ComputeTime-to-budget conversion was repeated as hard-coded literals across admission, donation and exact binding verification. Added one pure `ResourceEnvelopeBudgetMapping` and routed those paths through it.
- The mapper supports only the exact locally executable `ComputeTime/Nanoseconds → ComputeTimeNanoseconds` relation. DMA/network/fabric/device-memory/queue/inflight resources are provider-local or lack an exact SingNext budget dimension and fail closed rather than aliasing unrelated dimensions.
- Existing atomic reservation requires no redesign. P14 must measure its single lock before any sharding proposal.
- Remaining: no executable provider-local all-or-nothing acquisition/release-before-wait proof, so mixed OS/provider vector coordination remains FutureGated instead of receiving an escrow workaround.

## 5. C1-C12

C1/C2/C12: scheduler score/hint cannot authorize or refine a mismatch. C3: ResourceBudgetAuthority is the sole quantitative owner and vector mutation is atomic. C4: lease/mapping does not grant effect permission. C5/C6: reservation does not imply execution, completion, visibility or publication. C7: scheduler/provider/lease generations revalidate exactly. C8/C9: hints, evidence and replay authorize no submit. C10: no containment claim. C11: mapping preserves explicit resource class/unit and does not reinterpret retire/measurement.

## 6. Files and actual qualification

- `ResourceEnvelopeBudgetMapping.cs`: pure canonical exact mapping and explicit unsupported boundary.
- `ResourceAdmissionProtocol.cs`, `ResourceDonationProtocol.cs`, `RuntimeKernel.SemanticExecutionBinding.cs`: one mapping source feeding existing owner APIs.
- `ResourceEnvelopeBudgetMappingTests.cs`, `VNextPhase10ResourceSchedulerTests.cs`: malformed/provider-local/authority-surface and favorable-score mismatch regressions.
- Existing `ResourceBudgetAuthority.cs`, P13 family tests and donation tests prove atomicity/conservation without owner changes.

Results:

- focused P10/owner/donation/sentry lane: 44 passed, 0 failed;
- solution build: exit 0, 0 warnings, 0 errors;
- full non-GUI: SingPlus 1633 passed/2 skipped; platform 60; adapter 90; neutral runtime 58; aggregate 1841 passed, 2 skipped, 0 failed;
- historical qualification tuple hashes were corrected to current bytes; final manifest/JSON/diff validation recorded after synchronization.

## 7. Fallback/FutureGated/HybridCPU exclusions

The existing single-owner ComputeTime reservation/donation path is the fallback. Unsupported provider-local classes remain gate-OFF and cannot partially reserve an OS vector. A future provider contour must prove all-or-nothing admission or release-before-wait without adding a second SingNext ledger.

HybridCPU core, ISA, encoding, VLIW, pipeline, registers, pointers, typed lanes, compiler lowering, scheduler/retire architecture, memory/cache/coherence and microarchitecture were not changed. ISA impact: `NONE`.
