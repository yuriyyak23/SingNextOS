# P00 re-audit — exact live baseline and source freeze

Date: 2026-09-22 (Europe/Moscow)

## Disposition

- Phase: P00.
- Disposition: `CLOSED_WITH_CORRECTIONS` for the local SingNextOS evidence contour.
- Maximum justified claim: `StaticAdmission`; the focused executable tests below prove repository/toolchain guards, not provider, ISE, hardware, NativeAOT, resource enforcement, or production qualification.
- Semantic co-design gates: OFF/default behavior preserved. No production source or gate was changed in P00.

## Exact local tuple and dirty-tree preservation

- Current HEAD: `a0f70bc2bd031bd2b1138b2a01984309814bba5c`.
- Current tree: `c9e667fda0605d993c01060b7d7dc04b73cfac80`.
- Roadmap-recorded SingNextOS baseline: `cb6a94c314055e0712b8d9b8382ca1d146fe1c0e`, tree `b0a9e343df10a99e26adddbe57669ed2533d1b8e`.
- Start branch: `master`.
- Start `git status --short`: empty. There were no pre-existing/user-owned dirty changes.
- The recorded HybridCPU-v2 source SHA/tree were not locally source-verified: no separate HybridCPU-v2 source checkout was used. Only tracked package bytes and local external-tool/adapter source were inspected.

## Toolchain, packages, contracts, and hashes

- `global.json` pins .NET SDK `11.0.100-rc.1.26425.128`, `rollForward=disable`, prerelease allowed. The installed SDK exactly matches.
- `Directory.Build.props` uses C# 13 by default and preview only by explicit opt-in.
- `HybridCPU.ExternalRuntime.Contracts.1.14.0.nupkg`: 74,244 bytes; SHA-256 `b96e99bda066ee585b26a11cbfa7b68ce6bf44fc0006679483ccc1a4eeb678c2`; Git blob `2614d43f8a9e2dc8e0fef525c494d7023d4cc806`; NuGet SHA-512 equals the `1.14.0` lockfile content hash.
- `HybridCPU.ExternalRuntime.1.3.0.nupkg`: 2,765,291 bytes; SHA-256 `191a1976decaf607425b3f93378ba11446b32af2ebfd09dff45a26944e7e765f`; Git blob `d528fd497f141417d18b2bbbd1310dfa0913baa9`; NuGet SHA-512 equals the adapter lockfile content hash.
- `HybridCPU.ExternalRuntime.Contracts.1.3.0.nupkg`: 36,785 bytes; SHA-256 `7956596e820f2536542a73171205ed0b7a3366996bbdb2b51d95c2ae1fe3fc92`; present only as the runtime package dependency contour and not treated as the authoritative 1.14.0 contract surface.
- `src/Runtime/SingPlus.Runtime/packages.lock.json` resolves the contracts package exactly to `1.14.0`; `tools/HybridCpu_ExecutableAdapter/packages.lock.json` resolves runtime `1.3.0` and contracts `1.14.0` exactly.
- All 59 pre-existing entries in `SHA256SUMS.txt` matched local roadmap bytes before this additive evidence was created. Both roadmap JSON files parsed successfully.

## Requirement → owner → source → behavior → test → evidence → gate → claim

| Requirement | Owner | Live source/contract | Actual behavior/evidence | Test/lane | Gate and claim |
|---|---|---|---|---|---|
| CD-P00-01 exact source tuple | Git/local qualification owner | `.git`, `global.json` | Roadmap baseline is stale relative to current HEAD; both commit/tree tuples are recorded exactly | `git rev-parse`, `git cat-file`, baseline diff inventory | gates OFF; `StaticAdmission` |
| CD-P00-02 package and lock identity | Supply-chain/qualification owner | `.packages`, both `packages.lock.json`, exact `PackageReference`s | Local SHA-256, Git blob, and NuGet SHA-512 recomputed; lock hashes match | local hashing and JSON parsing | gates OFF; package identity only |
| CD-P00-03 test/evidence level | Qualification owner | four test projects; 186 `*Tests.cs` files at inventory time | 14 focused architecture/toolchain tests actually passed; no provider/ISE execution inferred | focused `dotnet test --no-restore` | gates OFF; `StaticAdmission` |

## C1–C12 and authority separation

P00 changes no authority or runtime path. C1–C12 are retained as constraints for later phases. Package presence, DLL presence, build output, test discovery, receipts, telemetry, manifests, and this evidence document are non-authoritative. Resource, effect, Region, operation, publication, provider-admission, runtime-legality, and planner ownership remain separate.

## Commands and actual results

- `git rev-parse HEAD` → `a0f70bc2bd031bd2b1138b2a01984309814bba5c`.
- `git rev-parse HEAD^{tree}` → `c9e667fda0605d993c01060b7d7dc04b73cfac80`.
- `git status --short` at phase start → no output.
- `dotnet --info` → pinned SDK installed and selected.
- Local SHA-256/SHA-512/Git-blob computation → values above; both lockfile content hashes matched.
- Roadmap `SHA256SUMS.txt` verification before additive files → `HASH_FAILURES=0`.
- JSON parse of `TASK_STATUS.json` and `PACKAGE_MANIFEST.json` → success.
- `dotnet test tests\SingPlus.Tests\SingPlus.Tests.csproj --no-restore --filter "FullyQualifiedName~SingCapPhase01ToolchainTests|FullyQualifiedName~VNextPhase00ArchitecturalFreezeTests"` → exit 0, 14 passed, 0 failed, 0 skipped. The .NET preview-support notice is informational.

## Defects corrected and remaining blockers

- Corrected: stale SingNextOS baseline is no longer treated as current evidence.
- Corrected: the two recorded key package SHA-256 values are now locally recomputed and lockfile-correlated.
- Corrected: P00 distinguishes test existence from the exact 14-test current run.
- FutureGated: HybridCPU-v2 source SHA/tree, its SDK `10.0.201`, provider/ISE behavior, hardware, QEMU, firmware, CXL boot, NativeAOT, and device guarantees lack exact local source/executable proof. Owner: respective HybridCPU source/runtime/provider qualification owners. Unsafe bypass would promote package metadata or host tests into runtime authority. Permitted fallback: local package/adapter contour with all semantic co-design gates OFF.

## HybridCPU scope and exclusions

HybridCPU remained a local external-tool/package/adapter contour. No HybridCPU core, ISA, encoding, VLIW, pipeline, registers, pointer model, typed lanes, compiler lowering, scheduler legality, retire architecture, memory controller, cache/coherence architecture, or microarchitecture was changed. ISA impact: `NONE`.
