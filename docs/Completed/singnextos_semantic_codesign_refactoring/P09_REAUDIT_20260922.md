# P09 re-audit — measurement identity and chargeability

Date: 2026-09-22 (Europe/Moscow)

## 1. Phase/disposition/claim/gates

- Disposition: `CLOSED_COMPUTETIME_HOST_SETTLEMENT_OTHER_CLASSES_STATIC_OR_FUTUREGATED`.
- Chargeability vocabulary/matrix: `StaticAdmission`; exact host/JIT `ComputeTime` evidence validation and existing budget settlement coordination: `RuntimeEnforced`.
- Provider measurement truth, HybridCPU execution, and settlement of throughput/occupancy classes are not proven. No `EnforcedUpperBound`, `GuaranteedReservation`, or `ProductionQualified` claim is made.
- All rollout gates remain OFF.

## 2. Baseline and reviewed scope

- HEAD/tree: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`; original P00 user-owned dirty set empty.
- Reviewed P09/TZ, C1-C12, task/traceability rows, `ResourceBudgetAuthority`, resource admission/binding/recovery code, platform resource contracts, and live settlement tests.
- HybridCPU package remains exact local 1.14.0 (`b96e99bda066ee585b26a11cbfa7b68ce6bf44fc0006679483ccc1a4eeb678c2`); package presence supplies no measurement or execution authority.

## 3. Requirement chain

| Requirement | Owner/live behavior | Evidence | Disposition |
|---|---|---|---|
| CD-P09-01 chargeability per class | immutable policy maps every `ResourceClassV1` to one unit and explicit charge events; policy exposes no authority/guarantee | every class/unit, execute/squash/replay/overhead/residency, retire/blocked exclusions | StaticAdmission closed |
| CD-P09-02 exact measurement identity | operation resource binding snapshots provider identity/generation plus measurement name/version/class/unit; evidence must match exactly | contract-version/provider/class/unit drift and malformed identity fail closed | RuntimeEnforced for local ComputeTime settlement input validation |
| CD-P09-03 replay/squash/retry/blocked semantics | checked normalization charges ComputeTime executed+squashed+replayed/retried+provider overhead; retired and blocked do not imply consumption; occupancy charges residency | normalization and overflow/unknown-version regressions | StaticAdmission; provider-side truth FutureGated |
| CD-P09-04 envelope/duplicate invariants | existing ResourceBudgetAuthority remains sole quantitative owner; normalized consumption must equal evidence and not exceed exact envelope; evidence ID/sequence terminal tuple is idempotent only when exact | over-envelope/dimension drift, reordered/cross-operation/provider drift, conflicting and concurrent duplicate tests | RuntimeEnforced exact host ComputeTime contour |

## 4. Defects/remediation and remaining risks

- Gap: usage evidence previously carried only an untyped scalar and receipt sequence. It now carries exact measurement-contract identity, non-empty evidence ID, and versioned event breakdown.
- Gap: retire, squash, replay/retry, provider overhead, residency and blocked time were not separated. A checked, dimension-specific policy now makes chargeability explicit and rejects unknown inputs/overflow.
- Duplicate acceptance now requires the exact terminal evidence ID, receipt sequence, and amount; measurement-contract drift is stale-generation failure.
- Existing budget owner rejects a terminal zero-charge settlement. No bypass or second ledger was added; exercised settlement evidence therefore contains non-zero executed consumption, while pure normalization proves retired/blocked-only values normalize to zero.
- Remaining: no executable provider measurement contract, SMT attribution source, HybridCPU replay/squash telemetry, or runtime settlement mapping for non-ComputeTime dimensions.

## 5. C1-C12

C1/C8: measurement/evidence authorizes nothing. C2/C12: planner/retire facts cannot legalize semantic mismatch. C3: normalized actual must equal evidence and stay within the exact lease envelope; measurement is evidence only. C4: resource accounting remains separate from effect authority. C5/C6: settlement does not imply visibility/publication/release. C7: operation, binding, provider and measurement generations/identities are exact. C9: duplicate/replay evidence cannot submit. C10: no containment claim. C11: chargeability is explicit and retired work is not consumption.

## 6. Files and actual qualification

- `ResourceMeasurementContracts.cs`: versioned identity, event breakdown and per-class checked chargeability matrix.
- `ExternalOperationResourceBinding.cs`: exact identity binding, normalization, envelope and terminal duplicate checks; ResourceBudgetAuthority remains the only settlement owner.
- `VNextPhase07ExternalOperationResourceBindingTests.cs`: class matrix, malformed/overflow, identity drift, normalization, duplicate/race and lifecycle regressions.

Results:

- focused measurement/settlement lane: 11 passed, 0 failed;
- solution build (`SingNextOS.slnx`): exit 0, 0 warnings, 0 errors;
- full non-GUI: SingPlus 1624 passed/2 skipped; platform 60; adapter 90; neutral runtime 58; aggregate 1832 passed, 2 skipped, 0 failed;
- JSON/manifest/hash and `git diff --check` validation are recorded after artifact synchronization.

## 7. Fallback/FutureGated/HybridCPU exclusions

The ordinary exact ComputeTime lease/settlement path remains the fallback. A provider without the exact measurement identity/breakdown is rejected; after possible submit ambiguity remains quarantined/reconciled, never automatically refunded or resubmitted. Other resource-class runtime settlement, real provider counters, SMT attribution and HybridCPU measurement enforcement remain FutureGated with provider/runtime owners and missing executable evidence stated above.

HybridCPU core, ISA, encoding, VLIW, pipeline, registers, pointers, typed lanes, compiler lowering, scheduler/retire architecture, memory/cache/coherence and microarchitecture were not changed. ISA impact: `NONE`.
