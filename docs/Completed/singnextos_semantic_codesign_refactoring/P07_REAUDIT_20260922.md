# P07 re-audit — effect traits and exact publication boundary

Date: 2026-09-22 (Europe/Moscow)

## 1. Phase/disposition/claim/gates

- Disposition: `CLOSED_EXISTING_HOST_PUBLICATION_OWNER_EXTERNAL_PROVIDER_ACTION_FUTUREGATED`.
- `EffectSemanticsV1` and its one-way mapper: `StaticAdmission`.
- Existing `ExternalOperationAuthority` staged/direct lifecycle and exact in-flight decision mechanics: `RuntimeEnforced` in the local host/JIT tests.
- Provider publication callback is a test action, not a real HybridCPU/provider contour; therefore no `ExecutableAdapter`, provider, ISE, hardware or ProductionQualified claim.
- All gates remain OFF; no new gate was needed because this repairs the existing owner path without activating a new contour.

## 2. Baseline and scope

- HEAD/tree: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`.
- P00 user-owned dirty set was empty; P00-P06 work remains preserved.
- Reviewed P07 phase/TZ, C1-C12, task/traceability rows, contracts, `ExternalOperationAuthority`, kernel wrapper, HybridCPU package publication ABI and lifecycle/provider tests.
- Exact package remains HybridCPU ExternalRuntime Contracts 1.14.0, SHA-256 `b96e99bda066ee585b26a11cbfa7b68ce6bf44fc0006679483ccc1a4eeb678c2`; no source/runtime execution claim follows.

## 3. Requirement chain

| Requirement | Owner/live behavior | Evidence | Disposition |
|---|---|---|---|
| CD-P07-01 orthogonal traits | immutable non-authoritative `EffectSemanticsV1`; existing lifecycle remains sole owner | invalid version/combination and false-authority tests | StaticAdmission |
| CD-P07-02 conservative mapping | pure one-way `ExternalEffectPolicy` mapper | all three classes mapped; no durability/compensation/commutativity invented | closed locally |
| CD-P07-03 exact decision/action | existing publication owner creates exact binding/dependency/policy/decision-generation value only at staged action boundary | staged exact callback, outside-lock query, cancel/provider-loss race rejection, failure discard; direct callback never runs | RuntimeEnforced host mechanics; provider action FutureGated |
| CD-P07-04 lifecycle matrix | existing staged/direct suite plus new boundary regressions | 24 focused lifecycle/provider tests passed | network/MMIO/durable real contours Unsupported/FutureGated |

## 4. Confirmed defects and remediation

- Defect: provider publication callback ran while the global `ExternalOperationAuthority` lock was held. Remediated with an owner-local `PublicationInFlight` winner and callback invocation outside the lock.
- Defect: direct/coherent lifecycle used the same callback-shaped publication path, implying fictitious withheld publication. Direct/coherent now records `DirectPublicationObserved` without invoking a publication action; its effect boundary was already irreversible/observable.
- Defect: no exact SingNext decision value reached the staged provider action. Added synchronous `ExternalPublicationDecisionV1` binding exact operation binding, dependency tuple, policy and owner decision generation. It explicitly is not reusable execution permission.
- Concurrent cancel, provider loss and release now fail while the exact publication action is in flight. Provider exception discards staged state and cannot be replayed as a fresh decision.

## 5. C1-C12

C1/C8: publication decision cannot authorize execution and traits authorize neither effect nor publication. C2/C12: no provider claim or planner decision participates. C3/C4/C11: no budget/measurement changes. C5: only staged contour invokes the exact SingNext publication decision; direct has no withheld gate. C6: execute/complete/visible/published/released remain distinct and the seven-state transition list is unchanged. C7: exact binding/dependencies/policy/decision generation are checked. C9: failed staged action cannot be resubmitted through decision replay. C10: no containment claim is added.

## 6. Changed responsibilities and qualification

- `contracts/SingPlus.Contracts/ExternalOperations.cs`: immutable traits, conservative mapper and exact synchronous decision value.
- `ExternalOperationAuthority.cs`: existing-owner publication winner, outside-lock callback, staged/direct separation and race exclusion.
- `RuntimeKernel.ExternalOperations.cs`: decision-aware overload while preserving ordinary API compatibility.
- `EffectPublicationSemanticsV1Tests.cs`: mapping, malformed inputs, exact decision, callback-lock, race, direct and failure tests.

Commands/results:

- focused lifecycle/provider lane: 24 passed, 0 failed;
- solution build: exit 0, 0 warnings, 0 errors;
- full non-GUI: SingPlus 1615 passed/2 skipped; platform 60; adapter 90; neutral runtime 58; aggregate 1823 passed, 2 skipped, 0 failed;
- historical tuple SHA/size was factually corrected where it pinned the changed live owner;
- JSON/hash/manifests and `git diff --check` are validated during final synchronization.

## 7. Fallback, blockers and exclusions

The ordinary public API remains compatible and delegates into the same owner. New semantic gates remain OFF. FutureGated owners/prerequisites: executable provider-specific staged publication action and receipt binding; network/MMIO/durable/compensatable contours; crash-durable in-flight reconciliation (P13). Test callbacks cannot prove any real provider behavior.

No HybridCPU core, ISA, encoding, VLIW, pipeline, registers, pointer model, typed lanes, compiler lowering, scheduler/retire architecture, memory controller, cache/coherence or microarchitecture was changed. ISA impact: `NONE`.
