# P05 re-audit — execution guarantees and exact semantic binding

Date: 2026-09-22 (Europe/Moscow)

## 1. Phase, disposition, claim, and gates

- Phase: P05.
- Disposition: `CLOSED_LOCAL_CONTRACT_AND_BINDING_WITH_HYBRIDCPU_SOURCE_FUTUREGATED`.
- Maximum claim: `StaticAdmission` for the exact local HybridCPU Contracts 1.14.0 compatibility mapping and immutable SingNext binding.
- No `RuntimeEnforced`, `ExecutableAdapter`, resource upper-bound/reservation, provider execution, ISE or hardware claim is made.
- All semantic co-design gates remain OFF.

## 2. Baseline, packages, and preserved worktree

- HEAD/tree: `a0f70bc2bd031bd2b1138b2a01984309814bba5c` / `c9e667fda0605d993c01060b7d7dc04b73cfac80`.
- Original user-owned dirty set at P00: empty. P00–P04 changes and factual historical hash corrections were preserved.
- Exact local package remains `HybridCPU.ExternalRuntime.Contracts` 1.14.0, SHA-256 `b96e99bda066ee585b26a11cbfa7b68ce6bf44fc0006679483ccc1a4eeb678c2`; lockfile resolves exactly 1.14.0.
- Local package README/PublicApi and compiled ABI were reviewed. HybridCPU source worktree remains unavailable, so source SHA/reachability and source-level runtime/ISE behavior are not claimed.

## 3. Requirement → owner → source → behavior → tests → claim

| Requirement | Owner | Live implementation/behavior | Executed evidence | Claim |
|---|---|---|---|---|
| CD-P05-01 guarantees descriptor | provider/runtime claims only; never OS authority | `ExecutionGuaranteesV1` has 13 dimension-specific claims, explicit support, claim ceiling and evidence source; supported claims cannot omit a source; unsupported claims must remain ModelOnly/None | canonical/tamper/evidence-source/authority-surface tests | `StaticAdmission`; HybridCPU package source extension FutureGated |
| CD-P05-02 exact binding | SingNext coordination, no ledger | binding correlates operation, obligations/guarantees digests, exact live budget lease/envelope, provider identity, opaque `ExternalGenerationSet`, package/schema tuple, measurement identity and publication requirements | positive revalidation; provider drift, lease cancellation, wrong envelope, cross-operation replay and tamper negatives | `StaticAdmission`; not submit authority |
| CD-P05-03 1.14.0 mapping | compatibility adapter | only independent CPU/provider admission contract semantics, publication precondition, cancellation taxonomy, fail-closed generations and distinct lifecycle evidence map as supported; maximum per-dimension claim is StaticAdmission | exhaustive dimension-policy assertions and package constructor unknown-value failure | exact local package mapping only |
| CD-P05-04 ABI/conformance | CI/qualification | SingNext references the exact package only; package ABI references no SingPlus types and exposes no OS/resource authority DTOs | 24-test cross-ABI/provider lane + 5 package/hash tests | local package ABI evidence, not cross-repo source proof |

## 4. Conservative 1.14.0 negative space

The mapping explicitly leaves measurement, resource enforcement/assurance, isolation, contention, replay enforcement, determinism, containment and locality `Unsupported`/`ModelOnly`. Contract replay classification is not replay enforcement. Publication evidence-gate semantics are not a SingNext publication decision. Cancellation taxonomy is not closure. Lifecycle receipts are not provider execution proof.

Opaque provider generation tokens remain opaque: the binding stores the exact package object for equality and a digest of its strict JSON representation. No token ordering or private topology is inferred.

## 5. C1–C12 review

- C1/C4/C8: guarantees and binding have false authority/admission/publication flags and no mutation methods; provider claims do not authorize SingNext work.
- C2/C12: guarantees are inputs to later pure refinement; no planner override or submit path was added.
- C3/C11: the binding references the existing exact lease and envelope; 1.14.0 measurement/resource guarantees remain Unsupported.
- C5/C6: staged publication precondition and distinct lifecycle evidence are claims only; SingNext publication authority remains unchanged.
- C7: operation, obligations, guarantees, lease, envelope, provider identity and exact opaque generation set are bound and revalidated.
- C9: replay classification/evidence cannot authorize another execution.
- C10: containment remains Unsupported; no global epoch or closure claim exists.

## 6. Defects/remediation and remaining risks

- Gap: no uniform dimension-specific guarantee claim/evidence descriptor. Remediated locally in provider-neutral SingNext contracts.
- Gap: no exact immutable correlation covering owner requirements, provider claims, lease and opaque provider generations. Remediated with digest-protected binding and revalidator.
- Gap: package metadata could be overread as enforcement. Remediated with a strict 1.14.0 allowlist and explicit Unsupported dimensions.
- Remaining blocker: no locally available HybridCPU source permits adding/validating a new contract there. Unsafe bypass: editing the nupkg or claiming compiled ABI equals source verification. Safe fallback: local adapter mapping only.
- Remaining blocker: binding is not yet a mandatory submit gate and does not itself contain provider admission or runtime-legality decisions. Owner: P06 sentry.

## 7. Changed files and actual qualification

- `contracts/SingPlus.Contracts/ExecutionGuarantees.cs`: vocabulary, claim/evidence validation and digest.
- `src/Runtime/SingPlus.Runtime/VNext/HybridCpu114SemanticCompatibility.cs`: conservative mapping, opaque-generation digest and immutable binding contract.
- `src/Runtime/SingPlus.Runtime/VNext/RuntimeKernel.SemanticExecutionBinding.cs`: live binding construction/revalidation.
- P05 contract/runtime tests plus additive evidence/tuple and synchronized traceability/manifests.

Commands/results:

- focused new P05 tests: 10 passed;
- cross-ABI/provider/owner lane: 24 passed;
- exact package/hash lane: 5 passed;
- solution build: exit 0, 0 warnings, 0 errors;
- full non-GUI: `SingPlus.Tests` 1601 passed/2 skipped; platform HybridCPU 60; executable adapter 90; neutral runtime 58. Aggregate: 1809 passed, 2 skipped, 0 failed;
- JSON, manifests, SHA256 and `git diff --check` validated after synchronization.

## 8. Fallback, FutureGated, and HybridCPU exclusions

All new types remain inert while semantic gates are OFF; ordinary admission remains the fallback. Unknown package versions/classes, changed opaque generations, stale leases and mismatched digests fail closed.

FutureGated: HybridCPU source-level additive contract, actual provider admission decision binding, independent runtime-legality decision, pure full-dimension refinement and mandatory final sentry. Missing source/runtime evidence prevents any provider/ISE/execution/hardware claim.

HybridCPU core, ISA, instruction encoding, VLIW, pipeline, registers, pointer model, typed lanes, compiler lowering, scheduler legality, retire architecture, memory controller, cache/coherence and microarchitecture were not changed. ISA impact: `NONE`.
