# P14-5A evidence — conservative barrier classification

Disposition: `StaticAdmission`; `FG-BARRIER-MODEL` remains OFF. No executing barrier contour is enabled.

Baseline HEAD `52ccf45c05498a9143a599bb54499919a2cbcf8c`; Windows x64 JIT; SDK `11.0.100-rc.1.26425.128`; runtime `11.0.0-rc.1.26425.128`. HEAD/status were captured before the phase and the pre-existing dirty worktree was preserved.

Strict repeat-audit qualification HEAD is `6227ea7cf258ef6ffce52001d4d2ffee07355b35`, with source disposition `CurrentHeadPlusPreservedDirtyWorktree`. The repeat audit re-read the normative P14-5A requirements, `SipJobBarrierPlanner`, the plan verifier/strict JSON parser, default-gate table, focused tests, claim matrix and tuple. It made no reset, checkout, clean, commit, push, remote operation or user-file deletion.

`SipJobBarrierPlanner` is a closed version-1 classifier. `None` is the only fusible value and still requires live revalidation. External effect, publication, ownership settlement, async wait, cross-runtime, independent cancellation, observable invocation, irreversible private mutation, and unsupported authority commit produce an ordinary-SIP materialization decision naming the existing lifecycle owner. Cross-runtime always names ordinary transport. NativeIsolated and ConfidentialDomain reject as FutureGated. Unknown version/value rejects as `UnknownBoundary`; it can never degrade to `None`.

The decision is non-authoritative metadata: it contains no capability, pin, lease, reference, historical authorization result, provider handle, or implementation target. P14-1 still rejects non-`None` edges in the executable linear MVP, so this phase cannot accidentally enable execution.

Executed evidence:

- focused classifier plus plan-verifier regressions: 42 passed, 0 failed;
- Runtime project build: 0 warnings, 0 errors;
- full solution build: 0 warnings, 0 errors;
- full non-GUI suite: main assembly 1298 passed, 8 unchanged unrelated failures, 2 skipped (1308 total); other assemblies 90/90, 58/58, and 60/60 passed. The failures remain seven absent historical JSON artifacts and one pre-existing security-profile inventory mismatch; no P14 test failed.

Current repeat-audit lane (`Phase145ABarrierPlannerTests`, P14-1 verifier, P14-0 default-gate policy and P14-8 closure) passed 56/56. The immediately preceding exact full non-GUI run on the same rebuilt executable tree recorded 1350 passed, the same 8 unrelated failures, and 2 skipped in `SingPlus.Tests`; Runtime and solution builds were 0-warning/0-error. Both roadmap JSON files, all seven tuple artifact hashes, tuple-to-HEAD binding and `git diff --check` were revalidated. No implementation defect or claim promotion was found in this pass.

`FG-BARRIER-MODEL` remains default OFF. Exact claim is static, conservative classification only; runtime materialization execution remains ordinary SIP. `NativeIsolated` remains `FutureGated`, owned by the existing isolated-transition/runtime-boundary owners: the exact missing prerequisite is a production Job-to-isolated-boundary materialization contract defining closed input/output projection, identity/generation revalidation, cancellation/fault settlement and reference isolation, plus executable ordinary-versus-Job evidence for that exact contract. `ConfidentialDomain` remains `FutureGated`, owned by the secure/confidential-domain transition owners: the exact missing prerequisite is a production secure-boundary Job materialization contract defining admitted closed state, owner identity/generation, completion/visibility/publication and cancellation/fault behavior, plus executable differential evidence for that contract. Neither contract/evidence exists in the current repository. Bypassing either prerequisite would erase an independently authoritative runtime/security boundary and could carry ambient authority, raw managed references, stale identity or unqualified completion across it; therefore the classifier rejects rather than fuses. No implementation or qualification claim is made for either item.

Ordinary SIP remains fallback/oracle; no raw ManagedCap reference crosses a Job boundary; plans/handles/caches do not confer authority. No HybridCPU core/ISE/ISA/compiler/scheduler-legality/microarchitecture code changed. QEMU, firmware, CXL boot, hardware, NativeAOT, confidential execution, and acceleration are unclaimed.
