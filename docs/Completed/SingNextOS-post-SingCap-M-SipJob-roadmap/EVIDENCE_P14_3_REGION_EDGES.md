# P14-3 evidence — Region edges

## P14-3A disposition

The immutable admission layer independently admits canonical linear read-only BORROW and two-transfer MOVE descriptors. Owner-composed qualification-only JIT contours for both now reach `RuntimeEnforcedTestOnly`; `FG-REGION-BORROW` and `FG-REGION-MOVE` remain OFF and there is no production Region Job executor. Final publication, production plan binding, complete transfer-hook permutations and NativeAOT evidence remain FutureGated.

Phase entry revalidated baseline HEAD `52ccf45c05498a9143a599bb54499919a2cbcf8c` and preserved the existing dirty worktree. Runtime/toolchain remains Windows x64 JIT on SDK/runtime `11.0.100-rc.1.26425.128` / `11.0.0-rc.1.26425.128`.

## Contract-first remediation

`SipJobRegionBorrowDescriptor` is immutable and non-authoritative. It records only the admitted intent: producer-response source role, read-only use, linear-consumer lifetime scope, and owner-defined `ReleaseUseThenReturnLoan` closure policy. It contains no Region handle, owner, generation, lease, view, backing reference, permission result or cached authorization.

The verifier accepts the descriptor only with the exact canonical gate set `FG-JOB-LINEAR`, `FG-REGION-BORROW` and exact edge semantics `BorrowRead` / `OwnerQualifiedView` / `InternalOnly` / `JobRun`. Unknown descriptor version, write mode, cached-owner source, unbounded lifetime, implicit closure, missing descriptor, Region MOVE, or non-canonical gate ordering fail before any runtime binding. Closed-copy edges cannot smuggle a Region descriptor. The complete descriptor is included in the canonical plan digest.

Focused evidence: `Phase141PlanVerifierTests` — 28 passed, 0 failed. Combined P14-1/P14-2 contour regression — 35 passed, 0 failed. SipJob plus directly relevant Region-use/hardening/IPC/ownership-ingress regression — 81 passed, 0 failed. The new tests prove canonical read-only BORROW admission does not enable the runtime gate, semantic mutations and an unused gate fail closed, the digest differs from the closed-copy plan, and metadata contains no authorization result. Full solution build succeeded with 0 warnings and 0 errors.

Full non-GUI suite: 1256 passed, 8 failed, 2 skipped, total 1266. The eight failures are unchanged and unrelated: missing historical P11/P12/P13 and Hybrid Boot evidence files plus the pre-existing security-profile inventory mismatch. They were not suppressed or rewritten.

## Runtime and ownership disposition

`RegionAuthority` remains the sole live owner. Existing `AcquireBorrowUse`, `ValidateUse`, `ReleaseUse`, `ReturnLoan`, `RevokeLoan`, `Transfer` and reclaim behavior were not copied or changed. No Job-local ownership ledger, transfer, inverse MOVE, retry, cleanup compensation or raw backing reference was introduced. Ordinary SIP remains fallback and the differential oracle.

Reviewing the live generated ABI exposed a prerequisite defect before runtime fusion: `[Borrows] OwnedBuffer<T>` was used unchanged as the parameter of `GeneratedSentryTarget` and `InvokeRuntime`. Ordinary transport projects that owner token to `BorrowLease<T>`; carrying `OwnedBuffer<T>` into a direct sentry would instead expose owner-bearing authority to the borrower. The generator now keeps `OwnedBuffer<T>` only on the caller-facing contract, client transport, dispatcher and ordinary `Invoke_*` entry, while the runtime sentry target and `InvokeRuntime_*` receive the owner-created `BorrowLease<T>`. This is baseline generated-sentry projection, not a Job authority path. Structural generated-source tests prove the split and continue to reject reflection, `object`, delegates and service locators.

Generator focused set: 33 passed, 0 failed. Generator/SipJob/Region regression set: 115 passed, 0 failed. Solution build: 0 warnings, 0 errors. The first full non-GUI run observed one unrelated DSC1 concurrency failure in addition to the known eight; the exact test then passed five consecutive isolated repetitions, and the immediately repeated full suite returned to 1256 passed, 8 failed, 2 skipped, total 1266. The recorded phase result is the repeatable eight known baseline failures; the transient DSC1 observation is retained here rather than hidden.

## Owner-owned inline BORROW foundation

`ChannelRegistry.BeginInlineBorrowInvocation<T>` now performs the same exact endpoint/process ownership, request shape, protocol legality, authoritative Region validation, `AcquireLoan` and owner-created `BorrowLease<T>` projection as ordinary `SendCore`, but creates no queue entry or envelope. It accepts exactly one read-only BORROW ownership payload, rejects consumes/pairs/copied shapes and message-attached capabilities, and advances protocol sequence/state only after the loan and safe projection succeed. Projection failure invokes the existing owner-defined `RevokeLoan`; no Job-defined compensation exists.

The returned `InlineBorrowProjection<T>` is internal TCB state containing only the protocol sequence and the existing owner-bound `BorrowLease<T>`. It contains no `OwnedBuffer<T>`, owner identity, generation cache, authorization result or backing storage reference. Consumer use is still acquired via `RuntimeKernel.AcquireBorrowRegionUse`; closure is explicit `ReleaseRegionUse` followed by `ReturnBorrow`. `Dispose` does not manufacture settlement.

Focused tests: 4 passed, 0 failed. They compare ordinary and inline Region state, mutation epoch, protocol state/sequence and projected bytes while proving the sole difference is one queued ordinary envelope versus none inline. They also prove owner access exclusion while loaned, use lifetime through last access, lease invalidation after explicit closure, borrower-termination reclaim invalidation, second-borrow/stale failure without generation guessing, and fail-before-loan/protocol-commit for unsupported or capability-bearing messages. Expanded generator/SipJob/Region/session regressions: 126 passed, 0 failed. Solution build: 0 warnings, 0 errors. Full non-GUI suite after this addition: 1260 passed, 8 failed, 2 skipped, total 1270; the same eight unrelated baseline architecture evidence/profile failures remain.

## Session/invocation composition and generated sentry

`RuntimeKernel.BeginInlineBorrowSessionInvocation<T>` composes existing owners without creating a new transaction manager. It resolves exact process and session generations, acquires and revalidates the existing `EndpointSessionPin`, asks `ChannelRegistry` for the owner-created BORROW projection, registers/delivers/accepts the existing invocation record, and then acquires an exact read-only Region-use descriptor. `RevalidateInlineBorrowSessionInvocation` performs live session-pin, loan and Region-use validation immediately before sentry entry. `SettleInlineBorrowSessionInvocation` validates the service principal before consuming its TCB-private lease and closes in the explicit order `ReleaseRegionUse` → `ReturnBorrow` → invocation settlement → session-pin release/deferred channel cleanup. The composite has no public `Dispose`, authority cache, rollback or compensation claim.

The review found and remediated a terminal cleanup defect: the Region-use acquisition failure path released its session pin but did not claim an already-requested deferred session/channel close. All inline pin release paths now use one owner-cleanup helper which releases the pin first, then asks `EndpointSessionRegistry.ClaimClosedCleanup` and closes invocation/channel state under the existing correlation boundary. It does not infer compensation for Region or invocation state.

The generated qualification contract proves the runtime ABI end to end. Ordinary SIP materializes a `BorrowLease<byte>` and invokes `InvokeRuntime_InspectAsync`; the inline contour passes the independently owner-created `BorrowLease<byte>` through the same operation-specific generated thunk. Both produce the same closed result. Reflection assertions prove the TCB-private binding holds no `OwnedBuffer`, `BorrowLease`, delegate or `object` field; the typed sentry target never enters a plan, frame, descriptor, cache projection or ManagedCap-visible result. The fused qualification path leaves Region `Owned`, its use `Released`, the session unpinned and the channel queue empty.

Focused `Phase143InlineRegionBorrowTests`: 8 passed, 0 failed. Directly relevant P14-2/P14-3, generator, protocol, Region-hardening and cancellation regressions: 89 passed, 0 failed. Runtime project build, test project build and full solution build each succeeded with 0 warnings and 0 errors. Full non-GUI suite: 1264 passed, 8 failed, 2 skipped, total 1274. The same eight unrelated baseline failures remain: seven missing historical P11/P12/P13 or Hybrid Boot evidence files and the pre-existing security-profile inventory mismatch; none was hidden or rewritten.

BORROW is still test-only and gate-disabled because a production plan-to-binding executor, full deterministic hook matrix around every owner linearization, cancellation/fault permutations and NativeAOT qualification are absent. MOVE separately requires the complete ordinary responder→caller→consumer transition trace, generation progression, stale handles and no inverse transfer. Owners: Region runtime, session/invocation owners and generated SIP sentry owners. No performance or zero-copy claim is made.

## Deterministic BORROW races and MOVE admission descriptor

An internal qualification-only hook vocabulary now exposes completed/safe boundaries outside authority-owner locks: after session pin, before session revalidation, after invocation acceptance, before/after Region-use acquisition, before/after final revalidation, before/after use release, borrow return and invocation settlement. It is not carried by a Job plan/frame/handle and is not a delegate-accepting ManagedCap API. The new deterministic tests use it without sleeps to force: close after pin but before revalidation; service termination immediately before final revalidation; cancellation after invocation acceptance; and close after use release but before loan return.

The first race exposed a second real cleanup defect: the pre-invocation failure path disposed the pin but did not ask `EndpointSessionRegistry` to claim a deferred close. Both copied-value and BORROW inline paths now release any remaining pin and complete deferred session/invocation/channel cleanup through the same existing owners. The tests prove zero remaining pins, closed channel generation, unchanged owner state before loan acquisition, stale lease/use after service termination, rejection of late cancellation, and ordered successful settlement when close races between use release and loan return.

MOVE admission is now independently contract-first. `SipJobRegionMoveDescriptor` contains only versioned intent and admits exactly `ProducerResponse` / `ResponderToCallerThenCallerToConsumer` / `NoImplicitInverseMove` under exact gates `FG-JOB-LINEAR`, `FG-REGION-MOVE`. Its complete contents are canonical-digest input and strict JSON parser input. Direct responder→consumer shortcuts, cached-owner source, fabricated rollback/inverse MOVE, BORROW sidecars, unknown versions and a MOVE gate without exactly one MOVE edge fail closed. The descriptor contains no owner, generation, Region handle, token or permission result. `FG-REGION-MOVE` remains OFF; this is `StaticAdmission`, not runtime MOVE qualification.

This iteration's focused verifier plus BORROW runtime set: 47 passed, 0 failed. Expanded P14-1/P14-2/P14-3, generator, ownership, Region-use, protocol and cancellation regression set: 164 passed, 0 failed. Runtime build, test build and solution build each succeeded with 0 warnings and 0 errors. Full non-GUI suite: 1275 passed, 8 failed, 2 skipped, total 1285; the same seven absent historical evidence artifacts and one pre-existing security-profile mismatch remain unrelated.

## Two-stage MOVE owner composition

The qualification-only MOVE contour now executes two operation-specific generated sentries and preserves both ordinary owner transitions. Stage A produces an `OwnedBuffer<byte>` owned by the responder. `ResponseRegistry.TransferInlineOwnership` is a transport-elision refactor of ordinary ownership publication: ordinary `Publish` and the inline path call the same authoritative validation/transfer/rematerialization helper. The inline entry additionally binds the exact session endpoint and message ID and validates that the exact response protocol declares the matching ownership payload; a non-ownership response contract fails before transfer. Stage B enters through `ChannelRegistry.BeginInlineMoveInvocation`, which preserves ordinary consuming-request shape, protocol transition, Region validation, caller→consumer transfer, old-token invalidation and process Region-set update while omitting only the queue/envelope.

The resulting completed sequence is exactly `producer@G → caller@G+1 → consumer@G+2`; there is no service-to-service shortcut. Both stages execute only through their generated `InvokeRuntime_*` sentries. The TCB-private MOVE lease contains the existing consumer-owned token plus session pin/invocation correlation and is not exposed through a plan, handle, cache or ManagedCap result. Settlement records invocation outcome but never synthesizes an inverse transfer.

Six focused MOVE tests pass. They prove ordinary-vs-inline result and ordered generation equivalence; invalidation of producer and intermediate caller tokens; consumer fault after the second committed transfer leaves the consumer authoritative; replay of the stale intermediate token fails; consumer termination before the second transfer leaves the caller authoritative at G+1; consumer termination after the second transfer uses existing owner reclaim and never restores caller ownership; and a non-ownership response contract cannot smuggle a Region transfer. This is a `RuntimeEnforcedTestOnly` owner-composition claim, not an enabled production Job contour: final Job publication/materialization integration, a full deterministic before/after-transfer hook matrix and NativeAOT qualification remain absent.

Latest focused MOVE set: 6 passed, 0 failed. Expanded P14-1/P14-2/P14-3, generator, response-protocol, ownership, Region-use, protocol and cancellation regressions: 171 passed, 0 failed. Runtime, test and full solution builds each succeeded with 0 warnings and 0 errors. Full non-GUI suite: 1281 passed, 8 failed, 2 skipped, total 1291; the unchanged seven missing historical evidence artifacts and one security-profile inventory mismatch remain unrelated.

`FG-REGION-BORROW` and `FG-REGION-MOVE` both remain OFF. Remaining P14-3 production prerequisites are exact plan-to-binding integration, final publication semantics, complete deterministic transfer/reclaim permutations and NativeAOT evidence. Owners remain `ResponseRegistry`, `ChannelRegistry`, `RegionAuthority`, session/invocation owners and generated SIP sentries. A direct A→B transfer remains forbidden.

## Repeat-audit settlement hardening (2026-09-20)

The repeat audit found a terminal-cleanup defect after the inline BORROW lease had become consumptive: `SettleInlineBorrowSessionInvocation<T>` returned immediately when `ReleaseRegionUse` or `ReturnBorrow` reported an owner error, which could skip the remaining reverse cleanup and invocation settlement even though the lease could no longer be retried. Settlement now attempts the complete owner-defined reverse sequence `ReleaseRegionUse` → `ReturnBorrow` → invocation settlement → pin/deferred-session cleanup, records success only when execution and both Region cleanup operations succeed, and returns the first owner error after all mandatory cleanup attempts. This does not fabricate rollback, restore ownership, or retry a consumed lease.

`StaleServiceDuringUseReleaseStillRunsReverseCleanupAndTerminatesLease` deterministically terminates the service at the safe `BeforeRegionUseRelease` hook. It proves that the implementation still reaches the post-release, borrow-return, and invocation-settlement boundaries; restores the Region to `Owned`; leaves the session pin count at zero; invalidates the BORROW lease; and rejects a second settlement attempt. The focused P14-3/P14-0/default-gate set passed 26/26, and the P14-3/P14-0/P14-8 closure set passed 27/27. The Runtime project and full solution builds both succeeded with 0 warnings and 0 errors. The exact full non-GUI command recorded 1350 passed, 8 failed, 2 skipped in `SingPlus.Tests`; the unchanged failures remain the seven absent historical evidence artifacts and one security-profile inventory mismatch already enumerated by P14-8. Current qualification HEAD was `6227ea7cf258ef6ffce52001d4d2ffee07355b35`; audit baseline remains `52ccf45c05498a9143a599bb54499919a2cbcf8c`, and the pre-existing dirty worktree was preserved.

This remediation remains `RuntimeEnforcedTestOnly`. It enables no production gate and adds no authority to a plan, handle, cache, trace or diagnostic. Ordinary SIP remains the fallback and semantic oracle; the operation-specific generated sentry remains the only service entry path. Production binding/publication, complete permutations and NativeAOT evidence remain exact missing prerequisites owned by the SipJob runtime/product, Region/session/invocation and generated-sentry owners; bypassing them would permit unqualified binding or stale owner state to reach execution.

No HybridCPU core, ISE, ISA/opcode, compiler-to-ISE, physical-register, frontend/pipeline/replay, memory-controller, retire-coordinator, scheduler-legality or microarchitecture implementation changed. QEMU, firmware, CXL boot, hardware execution and NativeAOT remain unclaimed.
