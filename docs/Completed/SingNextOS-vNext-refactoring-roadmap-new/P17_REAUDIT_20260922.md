# P17 sequential re-audit — 2026-09-22

P17 remains closed only for the exact Windows-x64/.NET 11/JIT host-provider migration contour at `RuntimeEnforced`; it is not `ProductionQualified`. Baseline `6227ea7cf258ef6ffce52001d4d2ffee07355b35` and reviewed HEAD `800027893dcf1ed23d8d7fe775841dd9d01a9fff` were rechecked with the pre-existing dirty tree preserved.

The additive mixed-version matrix, old-manifest canonical shape, old generated/ordinary SIP transport, optional pre-submit fallback, required-semantics denial, post-possible-submit quarantine, generation-bound rollback and no-live-consumer proof were reviewed against live code and tests. No compatibility path was removed. Focused P17 coverage passed 16/16 after remediation.

Final qualification built the full solution with 0 warnings and 0 errors. The complete non-GUI suite passed 1,767 tests with 0 failures and 2 existing explicit suspended-child skips (1,559 + 90 + 58 + 60). JSON parsing, tuple/manifest/traceability policy and diff checks were also rerun after the final hash cascade.

Two defects were confirmed in the non-authoritative compatibility usage registry. Generation exhaustion could throw, and release could mutate the live set before an overflowing generation increment. Registration and release now return `CapacityExhausted` before mutation. A null, short or noncanonical proof digest could also throw or enter comparison; `IsCurrent` now rejects it as `false`. Focused max-generation, partial-mutation and malformed-digest regressions cover both fixes.

The registry remains cleanup evidence only: it cannot mint, reserve, settle, submit, publish, reclaim or remove a compatibility path. A current empty-registry proof is necessary evidence, not authority to delete. Any future removal still requires a fresh production consumer inventory and exact proof; this local test registry is not a no-live-production-consumer claim.

All feature gates remain default OFF. Ordinary SIP/Compute/ExternalOperation fallback is preserved. HybridCPU source/provider mixed-version execution, NativeAOT, temporal upper-bound enforcement, guaranteed reservation, production rollout and hardware remain FutureGated because exact source, provider/environment and enforcement evidence are absent. No HybridCPU ISA or microarchitecture work occurred.
