# P01 sequential re-audit — 2026-09-22

## Disposition

P01 remains closed at `ModelOnly` for immutable `ResourceEnvelopeV1` and `ResourceUseConstraintV1` contracts on HEAD `800027893dcf1ed23d8d7fe775841dd9d01a9fff`. The P00 re-audit changes and all entry P16/P17 changes were preserved; no pre-existing path was reverted or attributed to P01.

## Reviewed chain

- Requirements: typed time/throughput/occupancy families, initial `ComputeTime/Nanoseconds`, checked canonical arithmetic, monotonic subset algebra and no sibling-union amplification.
- Owner: no mutable owner is added by P01. The contracts are descriptors; later live grants belong to `CapabilityAuthority` and quantitative state belongs to `ResourceBudgetAuthority`.
- Live source: `contracts/SingPlus.Contracts/ResourceControlModelContracts.cs`.
- Focused executable evidence: `SingPlus.Tests.Contracts.VNextPhase01ResourceModelTests`.
- CI lane: `eng/qualify-vnext.ps1`.
- Applicable model closure: VNX-002, VNX-005/006, VNX-023/024. Later direct-owner evidence remains authoritative for runtime parts of VNX-002/005/024.

## Positive and negative paths

Canonicalization rejects unknown version/family/class/unit/assurance, zero and `ulong.MaxValue` amounts, invalid validity intervals, whitespace/non-trimmed scope, incompatible family/class/unit/window tuples and checked window multiplication overflow. Subset tests cover reflexivity, generated transitivity, amount/validity/assurance/depth narrowing, dimension/scope mismatch and a sibling-union attempt exceeding the parent. JSON round-trip must be canonicalized before use.

No mutable grant, lease, reservation, settlement, provider binding, receipt or guarantee is materialized by these DTOs. There is no cross-family conversion or universal scalar.

## Commands and results

| Command | Result |
|---|---|
| `git rev-parse HEAD` | `800027893dcf1ed23d8d7fe775841dd9d01a9fff` |
| local ExternalRuntime package SHA-256 | `b96e99bda066ee585b26a11cbfa7b68ce6bf44fc0006679483ccc1a4eeb678c2` (inventory only) |
| HybridCPU source SHA lookup | absent locally; not verified |
| P01 focused test | 11 passed, 0 failed |
| current-tree affected/full builds | succeeded, 0 warnings, 0 errors |
| current-tree full non-GUI suite | 1,761 passed, 0 failed, 2 explicitly skipped |

## Gates, fallback and exclusions

All gates default OFF. `FG-VNX-RESOURCE-GRANT`, `FG-VNX-RESOURCE-LEASE`, throughput, occupancy, temporal and provider gates remain OFF for the P01 contour. Ordinary SIP/Compute/ExternalOperation behavior is unchanged; plans, caches, receipts and telemetry remain non-authoritative.

No defect requiring production-code remediation was confirmed. Excluded: live grant/lease enforcement, provider execution, temporal upper bound, guarantee, NativeAOT, HybridCPU/source/hardware qualification and ProductionQualified. No HybridCPU ISA, VLIW, pointer, register, lane, opcode, pipeline, replay, memory-controller, retire, scheduler-legality or microarchitecture work was performed.
