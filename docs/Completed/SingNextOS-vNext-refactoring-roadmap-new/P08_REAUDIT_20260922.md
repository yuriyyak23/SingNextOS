# P08 sequential re-audit — 2026-09-22

P08 remains `RuntimeEnforced` only for internal Windows-x64/.NET 11/JIT staged `ComputeTime/Nanoseconds` resource-aware admission on HEAD `800027893dcf1ed23d8d7fe775841dd9d01a9fff`. `FG-VNX-COMPUTE-V2` remains OFF and the ordinary resource-unaware ComputePlan path remains default.

ComputePlan and candidates remain policy/evidence only. Submit revalidates effect capability, resource-use grant, quantitative budget, exact Region uses, provider identity/generation and the independent CPU/runtime legality gate before P04 admission. No plan, candidate, cache, manifest, receipt or telemetry object grants authority.

A confirmed fail-closed defect was fixed. Duplicate live candidates with the planned `ProviderId` reached `SingleOrDefault` and threw `InvalidOperationException`. Validation now detects cardinality greater than one and returns `InvalidMessage` before admission. The focused regression proves no budget charge and that the ExternalOperation remains Prepared. Provider disappearance/fault, stale generation, incompatible semantics, revoked grants and each independent gate continue to deny without charge.

Qualification after remediation: focused P08 6 passed; planner/P04–P08 regression 57 passed; solution build 0 warnings/errors. Full non-GUI and closure results are pinned in the tuple after hash refresh.

The baseline exists locally; HybridCPU-v2 source is absent and package 1.14.0 is inventory only. P09 provider mapping/source qualification, NativeAOT, upper-bound/guarantee, provider/hardware and ProductionQualified remain FutureGated. No ISA or microarchitecture work occurred.
