# P04 sequential re-audit — 2026-09-22

P04 remains `RuntimeEnforced` for the internal Windows-x64/JIT process + independent effect/resource capabilities + ComputeTime lease + Region + ExternalOperation admission contour on HEAD `800027893dcf1ed23d8d7fe775841dd9d01a9fff`. `FG-VNX-CROSSOWNER-ADMISSION` defaults OFF.

`ResourceAdmissionProtocol` is a coordinator, not an authority. The reviewed sequence is validate owners -> reversible reserve -> exact revalidation -> acquire existing capability authority leases -> bind/admit -> unlock -> durably mark possible submit -> begin consumption -> provider callback. All provider/service callbacks execute after owner-local locks. Pre-submit failures compensate lease and operation; after possible submit, faults/provider loss quarantine without refund. Effect permission, resource permission, budget, Region, ExternalOperation, provider evidence and publication remain independent.

Focused review covers VNX-008/012–015/021/022/028: stale generations, revoke between validation/commit, Region mutation, fault at each reversible boundary, abandoned commit, duplicate submit, callback reentrancy, provider failure and deadlock stress. A confirmed coverage defect was fixed: `BeforeProviderCallback` and `AfterProviderCallback` hook failures were not in the prior “every boundary” tests. Two regressions now prove 0/1 callback invocation respectively and conservative quarantine with the full charge retained. Updated focused lane: 15 passed, 0 failed; broader cross-owner/durable lane: 23 passed, 0 failed; closure lane: 55 passed, 0 failed; solution build: 0 warnings/errors.

The post-change full non-GUI run is not reported as a pass: 1,762 passed, 1 failed and 2 skipped. The failure was `SingPlus.Tests.Platform.PlatformDsc1ComputeTests.ConcurrentEventObserversCallProviderAndPublishOnlyOnce` (“The local DSC1 submission does not exist”). It is outside the P04 files and then passed five exact reruns, but the failed full lane remains recorded and blocks any full-suite or production claim.

Crash/process-termination recovery is not inferred from exception injection; it remains the separately evidenced P16 journal contour. Session/invocation closure is deferred to P05/P06. All gates default OFF and ordinary SIP/Compute/ExternalOperation fallback remains unchanged. Package 1.14.0 is inventory-only and HybridCPU source is unavailable locally. No NativeAOT, upper-bound, guarantee, provider/hardware, ProductionQualified, ISA or microarchitecture claim/change is made.
