# P02 sequential re-audit — 2026-09-22

P02 remains closed at `RuntimeEnforced` only for the internal Windows-x64/JIT `CapabilityAuthority` resource-use grant contour on HEAD `800027893dcf1ed23d8d7fe775841dd9d01a9fff`. Entry P00/P01 re-audit outputs and pre-existing P16/P17 changes were preserved.

The reviewed chain is `ResourceUseConstraintV1` -> the existing `CapabilityRecord` -> `EffectiveCapabilityConstraints.IsSubset` -> live `ValidateResourceUse`/`AcquireResourceUseAuthority` -> focused capability tests -> `eng/qualify-vnext.ps1`. There is one capability record dictionary and no resource-capability ledger. `ResourceBudgetAuthority` remains the only later quantitative owner.

VNX-002/003/005/006/008/013 are satisfied for this exact internal contour: effect permission and resource permission deny independently; derivation can only narrow envelope, validity, assurance, scope and depth; exact subject/resource generations, revocation epoch and ancestor lineage are checked under the owner lock; DTO/JSON copies cannot validate without a live record. Derivation reserves no capacity and sibling declarative grants do not duplicate budget capacity.

Reviewed negative paths include unknown/canonical model inputs from P01, stale subject/resource generations, revoked grant, future validity, amount/scope/assurance widening, effect-only/resource-only substitution, serialized forgery, and derive-vs-revoke races. Focused plus directly relevant capability tests passed 16/16. Current-tree runtime/solution builds passed with zero warnings/errors; the current-tree full non-GUI suite passed 1,761 with 0 failures and 2 explicit skips. Package `HybridCPU.ExternalRuntime.Contracts/1.14.0` remains inventory-only at SHA-256 `b96e99bda066ee585b26a11cbfa7b68ce6bf44fc0006679483ccc1a4eeb678c2`; HybridCPU SHA `794c4a...` is absent locally.

`FG-VNX-RESOURCE-GRANT` defaults OFF because a grant alone must not bypass budget/effect/session/Region/provider admission. Ordinary fallback is unchanged. Plans, caches, receipts and telemetry remain non-authoritative. No provider, NativeAOT, temporal, guarantee, ProductionQualified, hardware, ISA or microarchitecture claim/change is made.
