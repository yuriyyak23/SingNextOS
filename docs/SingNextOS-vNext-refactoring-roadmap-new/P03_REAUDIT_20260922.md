# P03 sequential re-audit — 2026-09-22

P03 remains `RuntimeEnforced` only for the in-process Windows-x64/JIT `ResourceBudgetAuthority` lease state machine on HEAD `800027893dcf1ed23d8d7fe775841dd9d01a9fff`; `FG-VNX-RESOURCE-LEASE` defaults OFF.

The single `_reservations` store and `_gate` own reserve, split, bind, consuming, pre-submit cancellation, quarantine, reconciliation, settlement and release. Snapshots authorize neither effects nor reclaim. Exact reservation/process generations are checked. Terminal settlement/release is idempotent; post-possible-consumption release is denied; ambiguous work requires quarantine/reconciliation; split transfers existing capacity without incrementing ancestor usage.

VNX-004/007/010/012/023/024/028 were reviewed against the owner and tests. Last-unit contention, split-vs-consume, double terminal actions, oversettlement, stale generation, ID exhaustion, underflow prevention and process-retirement blocking are executable. Focused plus budget/recovery regressions passed 19/19. Current-tree builds and full non-GUI result remain 0 warnings/errors and 1,761 passed, 0 failed, 2 skipped.

Confirmed evidence defect: the historical phase/evidence lists crash-in-transition fault injection and owner-process restart as P03 obligations, but `VNextPhase03ResourceLeaseTests` contains neither. Later authenticated recovery-journal tests cover a separate P16 host recovery contour; they must not be back-projected into P03. Therefore P03 closure excludes crash/restart durability. Owner: `ResourceBudgetAuthority` recovery/journal integration. Missing at P03: fault injection at each mutation boundary and cold-process reconstruction/reconciliation evidence. Bypassing this gap with snapshots would resurrect authority or refund ambiguous consumption, so the contour stays FutureGated/OFF.

Package 1.14.0 remains inventory-only; HybridCPU reference source is absent locally. Ordinary fallback is unchanged; plans/caches/receipts/telemetry remain non-authoritative. No upper-bound, guarantee, provider, NativeAOT, ProductionQualified, hardware, ISA or microarchitecture claim/change is made.
