# P06 sequential re-audit — 2026-09-22

P06 remains `RuntimeEnforced` only for internal Windows-x64/.NET 11/JIT `ComputeTime/Nanoseconds` invocation-scoped donation on HEAD `800027893dcf1ed23d8d7fe775841dd9d01a9fff`. `FG-VNX-SESSION-DONATION` remains OFF; ordinary SIP is unchanged and default.

The reviewed owner chain is exact invocation/session binding in `EndpointSessionInvocationRegistry`, permission derivation/revocation in the existing `CapabilityAuthority`, and reserve/split/settle/quarantine in the sole quantitative `ResourceBudgetAuthority`. Root donation charges the caller, nested donation splits the same lease lineage, and generated sentry entry revalidates the exact invocation binding. Effect permission, resource permission, budget quantity, ExternalOperation, Region, provider evidence, visibility and publication remain independent. Donation records carry binding/provenance only and are not a second ledger.

A confirmed ABA/evidence defect was remediated. Nested provenance previously appended only downstream `ProcessId`; it omitted server/downstream process generations and downstream session/invocation identities and generations. Authority checks were still performed against the exact handles, but the claimed exact provenance was not itself exact. Nested provenance now includes both process generations and the full downstream session/invocation tuple. The focused regression asserts exact equality, so reuse of a numeric process/session/invocation ID cannot produce identical provenance.

Negative and race coverage verifies monotonic envelope/assurance/priority narrowing, exact caller/service/session/invocation generations, stale and ambient reuse rejection, duplicate bind denial, parallel split conservation, caller charging, no second sentry charge, pre-submit release, possible-submit quarantine, revocation and idempotent terminal cleanup. Focused P06: 6 passed. P02–P06 plus invocation regression: 46 passed. Runtime and solution builds: 0 warnings/errors. Full non-GUI after tuple refresh: 1,764 passed, 0 failed, 2 skipped.

The pinned baseline exists locally. HybridCPU-v2 source is absent; the local `HybridCPU.ExternalRuntime.Contracts/1.14.0` package is inventory only. P07 provider receipt/ExternalOperation reconciliation, production migration, durable external recovery, NativeAOT, upper-bound/guarantee, provider/hardware and ProductionQualified contours remain FutureGated. No HybridCPU ISA, VLIW, pointer/register/lane/opcode, compiler/ISE, scheduler, memory-controller, retire-coordinator or microarchitecture work occurred.
